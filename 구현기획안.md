# 얼음땡 — 구현 기획안

> **이 문서의 목적**
> 각 파트 담당자와 담당자의 AI 에이전트가 **이 문서 하나만 읽고** 작업에 들어갈 수 있게 쓴다.
> 추측이 필요한 자리를 남기지 않는다. 아직 안 정한 것은 §17 미결에 모아둔다.
>
> **서비스 이름** 얼음땡 *(가제)*
> **선행 문서** `기획정리.md` (서비스 정의 · MVP 기능) · `리포트시안.md` (리포트 표현 계층)
> **개정** v2 — 2026-08-27
> **팀** 진웅 · 기준 · 종훈 · 재환 · 치영 · 민우

---

# 0. 읽는 법 — 용어부터 맞춘다

**AI 에이전트에 넣을 때 이 절을 반드시 함께 넣는다.** 용어가 어긋나면 나머지가 전부 어긋난다.

| 용어 | 뜻 |
|---|---|
| **카테고리** | 방을 만들 때 고르는 상황. `팀플` `합석` `낮` `밤` 네 개 |
| **축 (Axis)** | 0.0~5.0 실수 하나로 표현되는 성향 차원 |
| **공통 6축** | 네 카테고리에 전부 존재. **정의는 하나, 값은 카테고리마다 따로 잰다** |
| **전용 3축** | 카테고리마다 다른 축 |
| **출처 (Source)** | 값이 어디서 나왔나. `SELF` `BEHAVIOR` `IMPRESSION` |
| **유형 (Type)** | 공통 3축으로 결정되는 8분면 중 하나. 이름과 대사가 붙는다 |
| **칭호 (Badge)** | 세션 안에서 뽑히는 배지. 유형과 **무관하게** 따로 산출 |
| **사전 (Dictionary)** | 카테고리별 표시 문구 묶음. **코드가 아니라 텍스트 파일** |
| **프레임 (Frame)** | 게임 진행 골격. `MANY`(다대다) `PAIR`(2인) 두 벌 |
| **스킨 (Skin)** | 카테고리별 캐릭터 변주. 베이스 8종에 소품·톤만 갈아끼운 것 |

> **축 개수를 세는 법이 헷갈리기 쉽다.**
> 시스템 전체 정의 축 = 공통 6 + 전용 12 = **18개**
> 한 사람이 한 세션에서 갖는 축 = **9개** (공통 6 + 그 카테고리 전용 3)

---

# 1. 서비스 한 줄 정의

> **처음 만난 사람들이 상황에 맞는 게임으로 서로를 알아가고, 그 결과가 내 성향 리포트와 캐릭터로 남아 이후 관계 게임에 계속 쓰이는 웹 서비스.**

**핵심 문장** — *게임으로 알아낸 것이 데이터로 남아서, 다음에 또 쓰인다.*
이 연결이 없으면 그냥 파티게임이고, 이 연결이 보이면 서비스가 된다.

**리포트는 두 겹으로 만든다.**

```
[상단]  유형 카드 · 칭호 · 궁합 등급 · 공유 카드     ← 바이럴 담당
[하단]  레이더 · 인식 격차 · 역할 추천 · 팀 리스크    ← 실용 담당
```

상단만 있으면 "단순 유흥"이 되고, 하단만 있으면 아무도 공유하지 않는다. **둘 다 필요하다.**

---

# 2. 확정 결정 로그

뒤집으려면 이 표를 고치고 아래 전부를 다시 본다.

| # | 항목 | 결정 | 영향 |
|---|---|---|---|
| D1 | 카테고리 구조 | **2단** — 단체(팀플·합석) / 연인(낮·밤) | 첫 화면 · 라우팅 |
| D2 | 축 구조 | 공통 6축 + 카테고리 전용 3축 | 데이터 모델 |
| D3 | 공통 축 값 | **카테고리마다 다시 잰다** | 저장 키에 카테고리 |
| D4 | 게임 프레임 | 다대다 기본, 2인은 축소 모드 | 프레임 2벌 |
| D5 | 오프라인 | **넣는다.** 저부담 · 3초 · 자리에서 안 일어남 | 액션 카탈로그 |
| D6 | 오프라인 데이터 | STT 안 씀. **직후 온라인 상호 응답**으로 뽑음 | IMPRESSION 출처 |
| D7 | 유형 톤 | **단일 톤(매운맛).** 순한맛 토글 없음 | 사전 1벌/카테고리 |
| D8 | 커버 장치 | 전 유형에 **강점 한 줄.** 공유 카드에도 노출 | 카드 레이아웃 |
| D9 | 칭호 | 한 사람당 **긍정 톤 최소 1개 보장** | 산출 로직 |
| D10 | F 등급 | **넣는다** | 궁합 등급 |
| D11 | 칭호 공개 | **팀 공개. 숨김 기능 없음** | 팀 리포트 |
| D12 | 밤 모드 | 동의 게이트 없음 · **스킵권 3장** · **공유 원본 그대로** | 밤 전용 |
| D13 | LLM | **판정 안 함, 문장만.** 세션당 1회, 폴백 있음 | 리포트 생성 |
| D14 | 구현 순서 | **팀플 수직 완성 → 나머지 수평 확장** | 일정 |
| D15 | 리포트 | **코드 1벌 + 사전 4벌** | 렌더 구조 |
| D16 | 캐릭터 | **베이스 8종 + 카테고리 스킨 4벌** | 카드 이미지 |
| D17 | 계정 | **익명 계정.** 로그인 없음 · `localStorage`에 `user_id` 하나 | 저장 구조 |
| D18 | 서비스 이름 | **얼음땡** *(가제)* | 카드 · 타이틀 |
| D19 | 문항 형식 | **2지 / 3지 / 순서형 4지 허용.** 리커트만 금지 | 문항 설계 |
| D20 | 궁합 범위 | **같은 세션 참가자끼리만** | 궁합 계산 · 페르소나 게임 |
| D21 | `PLN` 측정 | **순서 정하기**를 1단계에 삽입. MVP 포함 | 게임 |
| D22 | MBTI | **받지 않는다** | 시작 화면 |

---

# 3. 카테고리 — 2단 구조

```
                    ┌──────────────┐
                    │   방 만들기   │
                    └───────┬──────┘
              ┌─────────────┴─────────────┐
              ▼                           ▼
        ┌──────────┐                ┌──────────┐
        │   단체   │                │   연인   │
        └────┬─────┘                └────┬─────┘
      ┌──────┴──────┐             ┌──────┴──────┐
      ▼             ▼             ▼             ▼
  ┌───────┐   ┌───────┐       ┌───────┐   ┌───────┐
  │ 팀 플 │   │ 합 석 │       │  낮   │   │  밤   │
  └───────┘   └───────┘       └───────┘   └───────┘
   3~8인       4~8인            2인          2인
   MANY        MANY             PAIR        PAIR
```

| 카테고리 | 코드 | 인원 | 프레임 | 상황 | 부제 |
|---|---|---|---|---|---|
| **팀플** | `TP` | 3~8 | `MANY` | 조별과제 · 팀 프로젝트 | 같이 뭘 만들어야 하는 사이 |
| **합석** | `MT` | 4~8 | `MANY` | 과팅 · 헌팅포차 합석 | 오늘 처음 만난 남녀 |
| **낮** | `DY` | 2 | `PAIR` | 연인 초반, 서로 알아가기 | 사귄 지 얼마 안 된 사이 |
| **밤** | `NT` | 2 | `PAIR` | 연인, 더 가까워지기 | 둘만 보는 화면 |

**화면 규칙**

- **한 화면에 선택지가 셋 이상 안 나온다**
- `밤`은 **2단 진입 후에만** 보인다. 첫 화면에 노출하지 않는다
- 아직 안 채운 카테고리는 **비활성 + "준비 중"**. 숨기지 않는다

---

# 4. 저장 구조 — 익명 계정 `D17`

## 4-1. 로그인 없이 시작하되, 데이터는 서버에 남는다

```
첫 진입
   │
   ├─ localStorage에 user_id 있나?
   │    없음 → POST /users → 서버가 UUID 발급 → localStorage 저장
   │    있음 → 그대로 사용
   │
   ├─ 닉네임 · 성별 입력 → 서버 User 레코드에 저장
   │
   ▼
이후 모든 요청에 user_id 동반
```

**로그인 화면이 없다.** 그런데 축 · 유형 · 칭호 · 궁합 · 리포트는 전부 서버에 남는다.

## 4-2. `localStorage`에는 `user_id`만 넣는다

| 데이터 | 어디에 |
|---|---|
| `user_id` | **localStorage** — 이것 하나만 |
| 닉네임 · 성별 · 캐릭터 | 서버 |
| 축 점수 · 유형 · 칭호 · 궁합 | 서버 |
| 리포트 본문 | 서버 (LLM 1회 생성 후 저장) |

닉네임이나 방 코드까지 로컬에 넣으면 **서버와 어긋난다.** `user_id` 하나만 있으면 나머지는 서버에서 다시 받으면 된다.

```js
let id = localStorage.getItem('user_id')
if (!id) {
  id = (await post('/users')).user_id
  localStorage.setItem('user_id', id)
}
```

## 4-3. 새로고침 복구

```
F5 또는 모바일 브라우저가 탭을 죽였을 때

localStorage에서 user_id 읽음
   → GET /users/me?user_id=...        닉네임 · 성별 복원
   → GET /rooms/{code}                현재 단계 복원
   → 그 단계부터 이어서 진행
```

**전화를 받다가 탭이 죽는 건 흔한 일이다.** 이게 없으면 세션 중 새로고침 한 번에, 서버에 데이터가 멀쩡히 있는데도 다시 못 붙는다.

## 4-4. `user_id`를 잃는 경우

브라우저 데이터 삭제 · 다른 기기 · 시크릿 모드. 이러면 새 사람으로 시작된다.

**MVP에서는 복구하지 않는다.** 복구 링크나 계정 연동은 만들지 않는다. 트러블슈팅 문서에만 적는다.

> 로그인 없는 서비스가 그 이상을 보장하는 경우는 드물다. 복구는 P1에서 **로그인을 붙이면서** 해결한다. 그때 익명 `user_id`를 그대로 승격하면 데이터가 안 깨진다.

## 4-5. 모델

```
User                          사람 그 자체. 방이 달라도 유지된다
├─ user_id   : string (UUID)  PK
├─ nickname  : string
├─ gender    : string
└─ created_at

Participant                   이 방에서의 참가 기록
├─ id        : string  PK
├─ user_id   : string  FK →  User
├─ room_id   : string  FK →  Room
├─ is_host   : bool
└─ joined_at
```

> **`Participant.id`와 `user_id`를 반드시 갈라 둔다.**
> `Participant.id`는 방마다 새로 생긴다. 이것만 있으면 `AxisScore`가 사람을 찾아갈 수 없다.

---

# 5. 축 체계

## 5-1. 전체 지도

```
                   축은 18개 정의된다
     ┌──────────────────────────────────────────┐
     │  공통 6축     DOM SPD EXP PLN OPN EMP     │  ← 네 카테고리 공유
     ├──────────────────────────────────────────┤
     │  전용 3축 × 4  팀플 3 · 합석 3            │  ← 카테고리마다 다름
     │                낮  3 · 밤  3              │
     └──────────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
  ┌───────────────┐              ┌───────────────┐
  │     유 형     │              │     칭 호     │
  ├───────────────┤              ├───────────────┤
  │ 공통 3축만    │              │ 대부분 게임로그│
  │ DOM×PLN×EXP   │              │ 일부만 축 사용 │
  │               │              │               │
  │ 절대값        │              │ 팀 내 순위     │
  │ 남과 무관     │              │ 남과 비교      │
  └───────────────┘              └───────────────┘
```

> **핵심 대비 — 유형은 나 혼자 정해지고, 칭호는 팀이 있어야 정해진다.**
> 같은 사람이 다른 팀에 가면 **유형은 그대로, 칭호는 바뀐다.**

## 5-2. 공통 6축 — 정의는 하나

| 코드 | 내부명 | 0.0 쪽 | 5.0 쪽 |
|---|---|---|---|
| `DOM` | 주도성 | 맞춰간다 | 끌고 간다 |
| `SPD` | 속도 | 재보고 간다 | 일단 지른다 |
| `EXP` | 표현 | 변화구 — 돌려 말한다 | 직구 — 바로 말한다 |
| `PLN` | 계획성 | 감으로 | 정해두고 |
| `OPN` | 개방성 | 늘 하던 대로 | 새로운 걸로 |
| `EMP` | 공감 | 일 먼저 | 사람 먼저 |

### 카테고리별 표시 라벨 — 레이더에 들어가는 문구

| 코드 | 팀플 `TP` | 합석 `MT` | 낮 `DY` | 밤 `NT` |
|---|---|---|---|---|
| `DOM` | 판을 끌고 간다 | 대화를 돌린다 | 데이트를 제안한다 | 먼저 다가간다 |
| `SPD` | 일단 지른다 | 첫마디가 빠르다 | 마음이 빠르다 | 망설이지 않는다 |
| `EXP` | 할 말은 한다 | 돌려 말하지 않는다 | 좋으면 좋다고 한다 | 원하는 걸 말한다 |
| `PLN` | 정해두고 움직인다 | 2차를 정해둔다 | 계획한 데이트 | 미리 그려둔다 |
| `OPN` | 새로운 방식 | 모르는 쪽으로 간다 | 안 해본 데이트 | 새로운 시도 |
| `EMP` | 사람 먼저 | 분위기를 살핀다 | 기분을 먼저 본다 | 상대를 먼저 본다 |

> 라벨은 축의 **5.0 쪽**을 가리킨다. 레이더에서 바깥으로 갈수록 이 라벨에 가깝다.

> **합석 라벨은 이미 앉아 있는 상태를 전제로 쓴다.**
> "자리를 만든다" "자리를 짜온다" 같은 문구는 **합석이 성사되기 전** 얘기라 리포트에서 아무 정보도 못 준다.
> 합석 카테고리의 모든 라벨과 문항은 **앉은 다음부터 일어날 때까지** 안에서만 쓴다.

## 5-3. 카테고리 전용 3축

### 팀플 `TP`

| 코드 | 이름 | 0.0 | 5.0 |
|---|---|---|---|
| `TP_DDL` | 마감 스타일 | 스퍼트 — 막판에 몰아서 | 꾸준 — 나눠서 미리 |
| `TP_CFL` | 갈등 대응 | 회피 — 넘어간다 | 직면 — 그 자리에서 짚는다 |
| `TP_ROL` | 역할 선호 | 서포트 — 받쳐준다 | 리드 — 맡는다 |

> `TP_CFL`은 원래 회피/중재/직면 3분류였다. **축은 1차원이어야 하므로** 회피↔직면으로 두고 **2.5 부근을 중재형으로 해석**한다. 리포트 문구에서만 "중재형"으로 표시한다.

### 합석 `MT`

| 코드 | 이름 | 0.0 | 5.0 |
|---|---|---|---|
| `MT_RCT` | 리액션 볼륨 | 조용히 듣는다 | 크게 반응한다 |
| `MT_CRV` | 호감 곡선 | 알수록형 — 천천히 오른다 | 첫인상형 — 처음이 제일 세다 |
| `MT_STM` | 자리 지속력 | 일찍 빠진다 | 끝까지 남는다 |

### 낮 `DY`

| 코드 | 이름 | 0.0 | 5.0 |
|---|---|---|---|
| `DY_FRQ` | 연락 텀 | 뜸해도 괜찮다 | 자주 해야 한다 |
| `DY_AFF` | 애정 표현 | 행동으로 | 말로 |
| `DY_RCV` | 회복 속도 | 시간이 필요하다 | 그날 안에 푼다 |

### 밤 `NT`

| 코드 | 이름 | 0.0 | 5.0 |
|---|---|---|---|
| `NT_MOD` | 분위기 의존도 | 분위기가 있어야 한다 | 분위기랑 상관없다 |
| `NT_LED` | 리드·팔로우 | 맞춰준다 | 이끈다 |
| `NT_OPN` | 개방도 | 조심스럽다 | 거리낌 없다 |

> **`NT_PCE`(속도)를 뺐다. 공통 `SPD`와 같은 걸 재고 있었다.**
> 두 축이 같은 걸 재면 궁합 계산에서 그 방향이 이중으로 가중된다. 그리고 "속도"가 무엇의 속도인지 — 그날 진도인지, 관계 진전인지 — 읽는 사람마다 달라진다.
> `SPD` 밤 라벨을 **"망설이지 않는다"** 로 못박고, 전용 축은 겹치지 않는 **`NT_MOD` 분위기 의존도**로 교체했다.

## 5-4. 데이터 모델

```
AxisScore
├─ user_id      : string
├─ category     : "TP" | "MT" | "DY" | "NT"     ← D3 때문에 반드시 키에 포함
├─ axis_id      : "DOM" | ... | "NT_OPN"
├─ source       : "SELF" | "BEHAVIOR" | "IMPRESSION"
├─ value        : float 0.0~5.0
└─ session_id   : string
```

**PK = `(user_id, category, axis_id, source, session_id)`**
카테고리를 빼면 팀플에서 잰 주도성이 연인 리포트를 덮어쓴다.

### 출처별 용도 — 이 표를 어기면 안전 규칙 위반이다

| 출처 | 무엇에서 나오나 | 유형 결정 | 칭호 | 궁합 | 레이더 |
|---|---|:---:|:---:|:---:|---|
| `SELF` | 설문 응답 | **○ 0.6** | ○ | ○ | 실선 |
| `BEHAVIOR` | 게임 중 실제 선택·기록 | **○ 0.4** | ○ | ○ | 실선 |
| `IMPRESSION` | 익명 지목 · 상호 응답 | **✗ 절대 안 씀** | ○ | ✗ | **점선** |

> **`IMPRESSION`을 유형 결정에 넣으면 안 된다.**
> 그 순간 리포트가 *"팀은 당신을 ○○로 봤습니다"* 가 아니라 *"당신은 ○○입니다"* 가 된다.
> 남이 찍어준 인상을 사실로 확정하는 것 — **이 한 줄이 서비스를 죽인다.**

### 최종 축 값

```
axis_final(user, cat, axis) = SELF × 0.6 + BEHAVIOR × 0.4

BEHAVIOR 없으면 (중간 이탈 등)  →  axis_final = SELF
레이더 점선 = IMPRESSION        ←  별도. 절대 섞지 않는다
```

---

# 6. 유형 — 공통 3축으로만 뽑는다

## 6-1. 결정 규칙

```
DOM ≥ 2.5 ? 상 : 하
PLN ≥ 2.5 ? 상 : 하        →  2 × 2 × 2 = 8분면
EXP ≥ 2.5 ? 직구 : 변화구
```

점수 자체는 안 쓴다. **2.5를 넘었냐 아니냐만 본다.**

**타이브레이크** — 세 축 중 하나라도 `2.5 ± 0.3` 안이면 `SPD`로 가른다. `SPD ≥ 2.5`면 `상` / `직구` 쪽으로 민다.

## 6-2. 왜 이 3개인가

| 뺀 축 | 이유 |
|---|---|
| `SPD` 속도 | **`DOM`과 상관이 너무 높다.** 끌고 가는 사람은 대개 빨리 지른다 |
| `OPN` 개방성 | **눈에 안 보인다.** 게임 한 판으론 안 드러난다 |
| `EMP` 공감 | **문항을 잘못 짜면 전원이 높게 답한다.** §9-1 원칙 2를 지키면 갈리지만, 유형 축으로 쓰기엔 여전히 불안정하다 |

남긴 셋은 **게임 한 판 안에서 눈으로 확인된다.** 회의 5분이면 누가 끌고 가는지 보이고, 마감 때 계획형인지 드러나고, 갈등에서 직구인지 변화구인지 나온다.

## 6-3. 8개가 한계인 이유

축을 4개 쓰면 16유형, 5개면 32유형이다. **MBTI가 16개인데 그것도 이미 많다** — 대부분 자기 것만 알고 나머지는 모른다. 우리는 한 세션에 6명이 서로 유형을 보여주며 웃어야 하는데, 32개면 남의 유형이 뭔지 감이 안 온다. **8개여야 "아 쟤 그거구나"가 된다.**

## 6-4. 전용 3축은 유형에 안 쓰인다 — 그럼 어디에

```
① 궁합 계산      시너지 가중치 표에 9축 전부 들어감
② 칭호 일부      "팀의 평균값" "극단주의자"는 9축 전체를 봄
③ 리포트 서술    LLM이 인용하는 디테일          ← 실질적으로 가장 중요
```

**③이 같은 유형 안에서 사람을 가르는 해상도를 담당한다.**

```
같은 "선빵 불도저" 두 명

종훈   TP_DDL 1.0 막판 몰아서   TP_CFL 4.2 그 자리서 짚음   TP_ROL 4.5 맡음
민우   TP_DDL 3.8 나눠서 미리   TP_CFL 1.5 그냥 넘어감      TP_ROL 2.0 받쳐줌

→ 유형 이름은 같지만 리포트 본문은 전혀 다른 글이 나간다
```

## 6-5. 카테고리별로 뭐가 같고 뭐가 다른가

| | 네 카테고리가 **같은 것** | 카테고리마다 **다른 것** |
|---|---|---|
| 축 정의 | 공통 6축의 의미·방향·스케일 | 전용 3축 |
| 축 값 | — | **전부 다시 잰다** |
| 문항 | — | 4벌 |
| 유형 좌표계 | `DOM × PLN × EXP` 8분면 | — |
| 유형 이름 | — | 사전 4벌, 총 **32종** |
| 칭호 조건 | 단체 2개는 같음 | 연인 2개는 별도 (§7-3) |
| 궁합 계산식 | **코드 하나** | 가중치 표 4열 |

### 같은 좌표, 다른 이름

`상 · 하 · 직구` 한 칸이 카테고리마다 이렇게 달라진다.

| 카테고리 | 유형 | 대사 |
|---|---|---|
| 팀플 | 선빵 불도저 | "일단 만들어 왔어요" — *아무도 시킨 적 없음* |
| 합석 | 첫마디 담당 | "저희 게임할까요?" — *앉은 지 4분 만에* |
| 낮 | 지금 나와 | 계획은 없다 — *근데 이미 출발함* |
| 밤 | 속전속결 | 분위기 잡는 시간 — *없음* |

**좌표는 같고 어휘만 갈아끼운 것이다.** 그래서 코드는 하나고 사전만 네 벌이다.

> **축 값을 카테고리마다 다시 재므로, 같은 사람이 카테고리마다 다른 유형이 나올 수 있다.**
> 버그가 아니라 사양이다. 그 값으로 그 카테고리의 리포트를 만들면 되고, **카테고리끼리 비교하지는 않는다** (§8-1).

## 6-6. 캐릭터 — 베이스 8종 + 스킨 4벌 `D16`

```
베이스 캐릭터 8종      좌표(DOM×PLN×EXP)가 성격을 정한다. 이게 뼈대
       ×
카테고리 스킨 4벌      의상 · 소품 · 배경 · 톤만 갈아끼운다

= 화면에는 32종, 그리는 건 8장 + 소품 4세트
```

| 스킨 | 소품 · 톤 |
|---|---|
| 팀플 `TP` | 노트북 · 포스트잇 · 형광등 흰 톤 |
| 합석 `MT` | 술잔 · 조명 · 따뜻한 주황 |
| 낮 `DY` | 가방 · 커피 · 햇살 |
| 밤 `NT` | 실루엣 · 어두운 톤 |

**같은 캐릭터인 게 오히려 무기다.** *"쟤 팀플에서도 불도저였는데 연애에서도 불도저래"* 가 그림으로 바로 전달된다.

> **일러스트는 유형에만 붙는다. 칭호에는 안 붙는다.**
> 유형은 8칸 고정이라 8장이면 끝이지만, 칭호는 조합이 무한이라 그림을 못 그린다. 칭호는 **텍스트 배지**다.

## 6-7. 유형 32종 전문

### 팀플 `TP` — 조별과제 빌런 도감

| 조합 | 유형 | 대사 | 강점 한 줄 |
|---|---|---|---|
| 상·상·직구 | **일정 독재자** | "회의 5분만 할게요" — *2시간 함* | 이 사람 없으면 마감이 밀린다 |
| 상·상·변화구 | **총대 순교자** | "그냥 내가 할게" — *진짜로 다 하고, 절대 안 잊음* | 팀이 무너질 때 마지막까지 남아 있다 |
| 상·하·직구 | **선빵 불도저** | "일단 만들어 왔어요" — *아무도 시킨 적 없음* | 아무도 안 움직일 때 첫 삽을 뜬다 |
| 상·하·변화구 | **하이텐션 공회전** | 분위기 최고, 진도 0 — *웃다가 새벽 3시* | 이 사람 빠지면 팀이 조용해진다 |
| 하·상·직구 | **뒷자리 판사** | 본인은 안 함 — *남이 한 건 다 틀림* | 제출 직전에 틀린 걸 찾아낸다 |
| 하·상·변화구 | **잠수함** | 3일 읽씹 — *그리고 완성본 투척* | 조용하지만 결과물은 항상 나온다 |
| 하·하·직구 | **정직한 무임승차** | "나 이거 못 해" — *를 당당하게 말함* | 못 할 걸 미리 말해줘서 일정이 안 꼬인다 |
| 하·하·변화구 | **바람** | 회의엔 있었다 — *기억엔 없다* | 아무도 안 건드리는 잡일이 조용히 사라져 있다 |

### 합석 `MT` — 오늘 처음 만난 남녀

| 조합 | 유형 | 대사 | 강점 한 줄 |
|---|---|---|---|
| 상·상·직구 | **판 깔기 담당** | "자, 번호순으로 자기소개" — *도착 3분 만에* | 이 사람 없으면 40분 동안 폰만 본다 |
| 상·상·변화구 | **보이지 않는 손** | 자리 배치가 어느새 바뀌어 있다 — *누가 했는진 모름* | 어색한 조합을 조용히 갈아끼운다 |
| 상·하·직구 | **첫마디 담당** | "저희 게임할까요?" — *앉은 지 4분 만에* | 정적이 3초 이상 안 간다 |
| 상·하·변화구 | **텐션 유발자** | 웃기긴 함 — *무슨 얘기였는진 아무도 기억 못 함* | 이 사람 웃음소리로 자리가 산다 |
| 하·상·직구 | **품평단** | 본인은 말 안 함 — *끝나고 단톡에 총평 3장* | 다음 자리에서 뭘 고쳐야 하는지 안다 |
| 하·상·변화구 | **관찰 요원** | 조용히 다 보고 있다 — *나중에 다 맞힘* | 누가 누구한테 관심 있는지 제일 먼저 안다 |
| 하·하·직구 | **솔직 담백** | "저 이런 자리 처음이에요" — *진짜였음* | 이 한마디에 다들 긴장이 풀린다 |
| 하·하·변화구 | **배경 화면** | 분명 있었다 — *사진에도 있다* | 아무도 안 앉는 자리에 앉아준다 |

### 낮 `DY` — 사귄 지 얼마 안 된 사이

| 조합 | 유형 | 대사 | 강점 한 줄 |
|---|---|---|---|
| 상·상·직구 | **데이트 총괄** | 코스가 시간 단위로 짜여 있다 — *"여기서 12분 걸어"* | 이 사람이랑 나가면 헤맬 일이 없다 |
| 상·상·변화구 | **말없이 준비 완료** | 예약도 선물도 이미 다 됐다 — *말은 안 함* | 챙겨준 걸 나중에야 알게 된다 |
| 상·하·직구 | **지금 나와** | 계획은 없다 — *근데 이미 출발함* | 이 사람이랑은 지루한 주말이 없다 |
| 상·하·변화구 | **분위기 담당** | 뭘 했는진 기억 안 남 — *좋았던 건 확실함* | 같이 있으면 시간이 빨리 간다 |
| 하·상·직구 | **팩트 체커** | "그건 지난주에도 그랬어" — *기억력이 무기* | 이 사람은 약속을 안 잊는다 |
| 하·상·변화구 | **속으로 다 적음** | 아무 말 안 했는데 — *좋아하는 거 다 기억함* | 말한 적 없는 걸 알아서 챙긴다 |
| 하·하·직구 | **직진 무계획** | "나 오늘 뭐 하고 싶은지 모르겠어" — *를 솔직하게* | 이 사람한테는 눈치 볼 게 없다 |
| 하·하·변화구 | **흐름형** | 어디 갈지 안 정함 — *어디든 괜찮음* | 뭘 해도 불만이 없다 |

### 밤 `NT` — 둘만 보는 화면

> **수위 기준은 러브젠가.** 은근하게 두되 노골적으로 쓰지 않는다.

| 조합 | 유형 | 대사 | 강점 한 줄 |
|---|---|---|---|
| 상·상·직구 | **분위기 설계자** | 조명부터 재생목록까지 — *다 준비돼 있음* | 이 사람은 어색한 순간을 안 만든다 |
| 상·상·변화구 | **말없이 세팅** | 언제 했는지 모르게 다 돼 있다 | 티 안 내고 상대를 편하게 만든다 |
| 상·하·직구 | **속전속결** | 분위기 잡는 시간 — *없음* | 망설이다 놓치는 일이 없다 |
| 상·하·변화구 | **즉흥 담당** | 계획은 없었다 — *그래서 더 좋았다* | 예상 못 한 쪽이 항상 더 재밌다 |
| 하·상·직구 | **확인 사살** | "지금 괜찮아?" — *세 번 물음* | 이 사람이랑은 불편한 순간이 없다 |
| 하·상·변화구 | **눈치 백단** | 말 안 해도 안다 — *근데 자기 얘긴 안 함* | 상대가 원하는 걸 먼저 안다 |
| 하·하·직구 | **정직한 패스** | "오늘은 그냥 자자" — *를 미안해하지 않고* | 이 사람은 무리하게 만들지 않는다 |
| 하·하·변화구 | **맡김형** | 알아서 해줘 — *진심임* | 상대가 하고 싶은 대로 할 수 있다 |

## 6-8. 네이밍 원칙

1. **본인이 인정할 수 있어야 한다** — 억울하면 공유 대신 반박이 온다
2. **대사가 이름보다 웃겨야 한다** — 캡처되는 건 대개 대사 쪽이다
3. **남이 아니라 내가 웃겨야 한다** — 이 문구들은 전부 본인 설문 답변에서 나온다
4. **강점 한 줄에서 회수한다** — 세게 찌른 만큼 마지막 줄에서 돌려준다 `D8`

---

# 7. 칭호

## 7-1. 유형과 결정적으로 다른 점

```
유형     내 축 점수만 본다      →  혼자 있어도 정해진다
                                  다른 팀에 가도 그대로

칭호     팀 안에서 몇 등인가     →  나 혼자면 못 정한다
                                  팀이 바뀌면 칭호도 바뀐다

── 예 ──────────────────────────────────────
종훈의 소수파 선택 3회
   A팀 → 나머지가 0~1회      →  마이웨이 획득
   B팀 → 나머지가 4~6회      →  칭호 없음. 오히려 평범한 쪽
```

똑같은 행동인데 팀이 다르면 칭호가 다르다. 이게 *"우리 팀에서 내가 1위"* 를 만들고, **유형보다 칭호가 더 잘 퍼지는 이유다.**

## 7-2. 단체용 9종 — 팀플 · 합석

| 칭호 | 산출 조건 | 축 사용 | 단계 | 톤 | 리포트 표시 |
|---|---|:---:|:---:|---|---|
| **마이웨이** | 소수파 선택 최다 | ✗ | 1 | 중립 | 남들 갈 때 혼자 다른 데 감 |
| **말없이 정답** | 케미 최고 | ✗ | 2 | 긍정 | 말 안 해도 통함 |
| **텔레파시 불통** | 매치율 상위인데 케미 최저 | ✗ | 1·2 | 웃김 | 취향은 맞는데 생각은 안 맞음 |
| **숨은 공통점** | `me_too` 최다 수신 | ✗ | 3 | 긍정 | 꺼낸 얘기에 제일 많이 손 들림 |
| **미스터리** | 지목 정답률 최저 | ✗ | 4 | 긍정 | 아무도 못 맞힘 |
| **투명인간** | 지목 정답률 최고 | ✗ | 4 | 중립 | 다들 한 번에 맞힘 |
| **팀의 평균값** | 9축이 팀 평균에 가장 근접 | **○** | — | 중립 | 이 팀의 표준. 국룰 그 자체 |
| **극단주의자** | 9축 중 최고·최저 보유 최다 | **○** | — | 중립 | 뭘 해도 끝까지 감 |
| **첫인상 배신자** | 축 값 vs `IMPRESSION` 격차 최대 | **○** | 4 | 긍정 | 팀이 본 나와 실제 내가 제일 다름 |

**9종 중 6종은 축을 아예 안 본다. 게임 로그만 본다.**

### 산출 절차 — `D9` 긍정 보장 포함

```
1. 9종 조건 전부 평가 → 후보 목록
2. 사람당 최대 2개까지만
   ├ 3개 넘으면 자랑이 되고, 자랑은 안 퍼진다
   └ 우선순위: 긍정 > 웃김 > 중립
3. 긍정 보장 (D9)
   ├ 남은 2개에 긍정 톤이 하나도 없으면
   ├ 긍정 3종(첫인상 배신자 / 숨은 공통점 / 말없이 정답) 중
   └ 해당 지표가 가장 높은 것을 강제로 1개 부여
4. 쌍 칭호는 함께 노출
   └ 미스터리 ↔ 투명인간은 항상 같이 (한 명씩)
```

> **3번이 없으면 한 사람에게 중립·웃김만 몰릴 수 있다.**
> 그 사람은 카드를 안 올린다. 안 올리면 바이럴이 안 된다.
> **칭호는 팀에 공개되고 숨길 수 없으므로**(`D11`) 이 규칙이 유일한 완충 장치다.

## 7-3. 연인용 10종 — 낮 · 밤

**단체용 9종은 2인에서 작동하지 않는다.** 칭호가 "팀 내 순위"인데 두 명뿐이면 순위가 무의미하다.

```
6명 중 소수파 최다   →  의미 있음
2명 중 소수파 최다   →  동전 던지기
```

그래서 **순위형이 아니라 조건 충족형**으로 짠다. 전부 쌍으로 붙는다.

| 칭호 | 조건 | 톤 |
|---|---|---|
| **텔레파시** | 1단계 동시 선택 8개 이상 일치 | 긍정 |
| **평행선** | 1단계 일치 3개 이하 | 웃김 |
| **독심술사** | 2단계 예측 4개 이상 적중 | 긍정 |
| **아직 모르겠어** | 2단계 예측 1개 이하 적중 | 웃김 |
| **먼저 손 내민 쪽** | 3단계 오프라인에서 먼저 움직임 | 긍정 |
| **기다리는 쪽** | 3단계에서 상대를 기다림 | 중립 |
| **정반대** | 9축 중 3개 이상이 격차 3.0 이상 | 웃김 |
| **복사본** | 9축 중 5개 이상이 격차 0.5 이내 | 긍정 |
| **둘 다 걸었다** | 4단계에서 둘 다 `[함께]` | 긍정 |
| **한 명만 걸었다** | 4단계에서 한 명만 `[함께]` | 웃김 |

`D9`(긍정 최소 1개)는 여기도 동일하게 적용한다.

> **임계값에 근거가 없다. 실측이 필요하다** (§17 O5).
> 2인은 모수가 2명이라 순위로 못 뽑고 **절대 임계값에 전적으로 의존한다.**
> 너무 빡세면 아무도 못 받아 리포트가 텅 비고, 너무 헐거우면 전부 받아서 안 퍼진다.
> **목표는 칭호당 발생률 20~40%.**
>
> 참고 계산 — 이지선다 10개에서 두 사람이 무작위로 일치할 기대값은 5개다. 8개 이상은 약 5.5%로 **너무 빡세다.** 다만 커플은 취향이 비슷해 기대값이 6~7로 올라가므로 8이 적정일 수도 있다. **그 차이를 재야 한다.**

---

# 8. 궁합

## 8-1. 범위 — 같은 세션 참가자끼리만 `D20`

> **궁합은 같은 세션에 함께 참가한 사람끼리만 나온다.**
> 다른 방에서 만난 사람과의 궁합은 계산하지 않는다. 축 값을 카테고리마다 다시 재기 때문에 비교 기준이 없다.
> 페르소나 게임의 궁합보기도 **같은 세션 참가자 중에서만** 상대를 고를 수 있다.

## 8-2. 계산식

```
axis_synergy = Σ w[cat][axis] × f(dir[cat][axis], a, b) / Σ w[cat][axis]

f(SAME, a, b) = 1 − |a − b| / 5      비슷할수록 높음
f(DIFF, a, b) =     |a − b| / 5      다를수록 높음

total = match_rate    × 0.30       1단계 매치율
      + chemistry     × 0.20       2단계 케미 (짝이 아니었으면 팀 평균)
      + axis_synergy  × 0.50
```

## 8-3. 등급 컷과 카테고리별 이름

| total | 등급 | 단체 `TP` `MT` | 연인 `DY` `NT` |
|---|---|---|---|
| ≥ 0.80 | **S** | 소울메이트 | **환상의 궁합** |
| ≥ 0.65 | **A** | 척하면 척 | 말 안 해도 통함 |
| ≥ 0.45 | **B** | 무난 | 평범한 사이 |
| ≥ 0.30 | **C** | 노력형 | 맞춰가는 중 |
| < 0.30 | **F** | 상극 | **환장의 궁합** |

**F 등급이 있어야 대화가 시작된다** `D10`. 전부 좋게 나오면 아무도 안 본다.

> **연인은 환상 ↔ 환장이 대구를 이뤄서 F가 안 아프다.**
> 팀플의 "상극"은 좀 무거운데 "환장의 궁합"은 웃으면서 캡처한다.
> 단 **연인 등급 문구는 반드시 수치를 인용한다.** 두 명뿐이라 비교 대상이 없어서, 근거가 없으면 등급이 그냥 판정으로만 보인다.

## 8-4. 방향 · 가중치 표

**이 표가 카테고리별 차이의 전부다. 코드는 하나다.**

| 축 | 팀플 `TP` | 합석 `MT` | 낮 `DY` | 밤 `NT` |
|---|---|---|---|---|
| `DOM` | DIFF · 1.2 | DIFF · 1.0 | DIFF · 1.2 | DIFF · 1.3 |
| `SPD` | SAME · 0.8 | SAME · 1.0 | DIFF · 1.0 | DIFF · 1.2 |
| `EXP` | SAME · 1.2 | SAME · 1.2 | SAME · 1.3 | SAME · 1.2 |
| `PLN` | DIFF · 1.3 | SAME · 0.8 | SAME · 1.0 | SAME · 0.7 |
| `OPN` | SAME · 0.8 | SAME · 1.0 | SAME · 1.1 | SAME · 1.2 |
| `EMP` | DIFF · 0.9 | SAME · 1.0 | SAME · 1.3 | SAME · 1.3 |
| 전용 1 | `TP_DDL` SAME · 1.0 | `MT_RCT` SAME · 1.0 | `DY_FRQ` SAME · 1.2 | `NT_MOD` SAME · 1.2 |
| 전용 2 | `TP_CFL` SAME · 1.0 | `MT_CRV` SAME · 0.8 | `DY_AFF` SAME · 1.0 | `NT_LED` **DIFF · 1.1** |
| 전용 3 | `TP_ROL` DIFF · 1.1 | `MT_STM` SAME · 0.8 | `DY_RCV` SAME · 1.1 | `NT_OPN` SAME · 1.0 |

**읽는 법 몇 개**

- `TP / PLN = DIFF` — 팀플은 **계획형과 추진형이 섞여야** 굴러간다
- `TP / EXP = SAME` — 팀플이라도 **직구와 변화구가 만나면 오해만 쌓인다**
- `MT / DOM = DIFF` — 합석에서 둘 다 리드하려 들면 아무도 안 맞춰준다
- `NT / EMP = SAME · 1.3` — 밤 모드에서 공감 축이 갈리면 그날이 안 좋다
- `NT / NT_LED = DIFF` — 리드·팔로우는 **갈려야** 맞는다

> **"팀플은 달라야, 연애는 비슷해야"는 대체로 맞지만 축 단위로 갈린다.**
> 카테고리 단위로 한 방향을 정하면 오히려 틀린다. 그래서 축 × 카테고리 격자여야 한다.

> **부호 버그가 원천적으로 안 생기는 구조다.**
> 카테고리별로 식을 따로 쓰면 어느 한쪽은 반드시 `|a−b|` 부호를 틀리고, 그 버그는 예외를 안 던져서 출시까지 아무도 모른다.
> 이 표는 **데이터**라 개발자가 아니라 기획이 튜닝한다. **하드코딩 금지.**

## 8-5. 저장 키

```
CompatGrade
├─ user_a, user_b   : string      정렬해서 저장 (a < b)
├─ category         : string      ← 반드시 포함
├─ session_id       : string
├─ total            : float
└─ grade            : "S"|"A"|"B"|"C"|"F"
```

같은 두 사람이 팀플에서 S, 밤에서 F가 나올 수 있다. **버그가 아니라 사양이다.**

---

# 9. 문항 설계

## 9-1. 원칙 여섯 개

**1. 리커트 금지. 대신 세 형식을 허용한다** `D19`

```
✗ "1~5점으로 답하세요"  "얼마나 그런가"     ← 추상적 자기평가. 3~4에 몰린다

✓ 2지선다        기본
✓ 3지선다        축이 3단계로 자연스럽게 갈리는 상황
✓ 순서형 4지선다  시점 · 빈도 · 횟수처럼 자체 순서가 있는 것

✗ 4지 이상 비순서형                        ← 고를 근거가 없어 중간에 몰린다
```

리커트는 *추상적 자기평가*를 숫자로 묻는다. 3지·순서형 4지는 *구체적 행동*을 고르게 한다. **중간 선택지도 실제 행동이라 정직하게 갈린다.**

**2. 모든 선택지가 사회적으로 괜찮아야 한다**

```
✗ A 계획을 세워서 한다   B 계획 없이 한다        ← 전원 A
✓ A 할 일을 먼저 쪼갠다   B 일단 해보면서 정한다   ← 갈린다
```

3지선다가 특히 이 원칙을 살린다. 2지선다면 착한 쪽으로 몰리는 문항도, **중간 선택지가 도피처가 아니라 가장 실용적인 대응**이면 갈린다.

**3. 성향을 묻지 말고 상황을 준다**

```
✗ 당신은 주도적입니까?
✓ 조가 짜이고 첫 회의. 아무도 말을 안 꺼낸다.
```

**4. 축당 2문항, 그중 1개는 역채점**
같은 축을 반대 방향으로 한 번 더 묻는다. 두 답이 모순되면 중앙값(2.5) + `low_confidence`.
역채점 문항은 **A 위치가 축의 0.0 쪽**이다. 실제 채점은 각 선택지 옆 부호를 따른다.

**5. 한 문항은 한 축만 잰다**

**6. 카테고리 어휘를 쓴다**
같은 `DOM`이라도 팀플은 회의, 합석은 첫마디, 연인은 데이트 제안으로 묻는다.

> **순서형 4지선다는 시점이 그 축의 의미와 직결될 때만 쓴다.**
> 예를 들어 팀플 `PLN`에 "며칠 전부터 시작하나"를 쓰면 `TP_DDL`(마감 스타일)과 같은 걸 재게 된다.
> `PLN`은 *"미리 정해두느냐 상황 보고 정하느냐"* — **방식**이고, `TP_DDL`은 *"언제 하느냐"* — **시점**이다.
> 그래서 팀플 `PLN`은 2지선다, 낮 `PLN`은 순서형으로 갈렸다.

## 9-2. 채점

```
2지선다      A +1        B −1
3지선다      A +1  B  0  C −1
순서형 4지    A +1  B +0.33  C −0.33  D −1

축 원점수 = 해당 축 문항 합계          범위 −2 ~ +2
SELF(axis) = (원점수 + 2) / 4 × 5      범위 0.0 ~ 5.0

두 문항이 부호가 반대로 강하게 갈리면 → 2.5, low_confidence = true
```

## 9-3. 팀플 `TP` — 18문항

### 공통 6축 · 12문항

```
DOM-1  조가 짜이고 첫 회의. 아무도 말을 안 꺼낸다.
       A 일단 내가 뭐부터 할지 꺼낸다               DOM +
       B 누가 먼저 말하나 조금 기다린다              DOM −

DOM-2  역할을 나누는 중이다.                        [역채점]
       A 남는 자리를 맡는다                        DOM −
       B 내가 할 걸 먼저 고른다                     DOM +

SPD-1  아이디어가 하나 나왔다. 나쁘지 않다.
       A 일단 그걸로 가보자고 한다                  SPD +
       B 다른 것도 몇 개 더 보자고 한다              SPD −

SPD-2  자료를 찾다가 괜찮은 걸 발견했다.             [역채점]
       A 더 좋은 게 있는지 조금 더 본다              SPD −
       B 이거면 됐다 하고 덮는다                    SPD +

EXP-1  팀원이 가져온 결과물이 방향이 좀 다르다.
       A 다른 것 같다고 그 자리에서 말한다            EXP +
       B 일단 좋다고 하고 나중에 돌려 말한다          EXP −

EXP-2  회의 시간이 자꾸 길어진다.                    [역채점]
       A 답답해도 흐름이 끊길까 봐 참는다             EXP −
       B 그만하고 정하자고 한다                     EXP +

PLN-1  2주짜리 과제를 받았다.
       A 주차별로 할 일을 먼저 적는다                PLN +
       B 일단 시작하고 상황 봐서 정한다               PLN −

PLN-2  갑자기 일정이 하루 당겨졌다.                  [역채점]
       A 남은 걸 그때그때 쳐낸다                    PLN −
       B 계획을 다시 짜고 움직인다                   PLN +

OPN-1  전에 써본 적 없는 툴을 쓰자는 얘기가 나왔다.
       A 재밌겠다, 해보자                          OPN +
       B 익숙한 걸로 가는 게 안전하다                OPN −

OPN-2  작년 선배들 자료가 남아 있다.                 [역채점]
       A 그거 참고해서 비슷하게 간다                 OPN −
       B 보긴 보되 다른 방향으로 짠다                OPN +

EMP-1  한 명이 계속 늦고 진도가 안 나온다.            [3지선다]
       A 무슨 일 있는지 먼저 물어본다                EMP +1
       B 일정을 다시 맞추자고 한다                   EMP  0
       C 이대로면 못 한다고 짚는다                   EMP −1

EMP-2  결과물 퀄리티와 팀 분위기가 부딪힌다.          [역채점]
       A 퀄리티가 먼저다                           EMP −
       B 분위기 깨지면 어차피 안 나온다               EMP +
```

### 전용 3축 · 6문항

```
TP_DDL-1  마감이 2주 남았다.
          A 첫 주에 절반 이상 끝낸다                TP_DDL +
          B 마지막 3일에 몰아서 한다                TP_DDL −

TP_DDL-2  진도가 계획보다 앞섰다.                   [역채점]
          A 여유가 생겼으니 좀 쉰다                 TP_DDL −
          B 다음 것까지 당겨서 한다                 TP_DDL +

TP_CFL-1  회의에서 두 사람 의견이 부딪혔다.
          A 지금 정하고 가자고 한다                 TP_CFL +
          B 일단 넘어가고 나중에 본다                TP_CFL −

TP_CFL-2  누가 내 파트를 마음대로 고쳤다.            [역채점]
          A 별말 안 하고 넘어간다                   TP_CFL −
          B 왜 고쳤는지 물어본다                    TP_CFL +

TP_ROL-1  발표자를 정해야 한다.
          A 내가 한다고 한다                       TP_ROL +
          B 자료 만드는 쪽을 맡는다                 TP_ROL −

TP_ROL-2  팀장을 뽑는다.                           [역채점]
          A 하겠다는 사람 있으면 밀어준다             TP_ROL −
          B 아무도 안 하면 내가 한다                 TP_ROL +
```

## 9-4. 합석 `MT` — 18문항

```
DOM-1  자리에 앉았는데 아직 아무도 말을 안 한다.
       A 내가 먼저 뭐라도 던진다                   DOM +
       B 누가 시작하나 본다                        DOM −

DOM-2  게임하자는 얘기가 나왔다.                     [역채점]
       A 누가 룰 정하나 기다린다                    DOM −
       B 내가 룰을 설명한다                        DOM +

SPD-1  마음에 드는 사람이 생겼다.
       A 티 나게 그쪽으로 말을 건다                 SPD +
       B 티 안 나게 몇 번 더 본다                   SPD −

SPD-2  번호를 물어볼까 말까 싶다.                    [역채점]
       A 자리 끝날 때쯤 분위기 봐서                 SPD −
       B 생각났을 때 바로                          SPD +

EXP-1  상대가 한 말이 별로였다.
       A 아니라고 바로 말한다                       EXP +
       B 웃고 넘긴다                               EXP −

EXP-2  옆 사람이 계속 같은 얘기를 반복한다.           [역채점]
       A 그냥 들어준다                             EXP −
       B 다른 얘기 하자고 한다                      EXP +

PLN-1  2차를 갈 것 같은 분위기다.
       A 미리 검색해서 후보를 잡아둔다               PLN +
       B 나가서 보이는 데로 간다                    PLN −

PLN-2  집에 갈 시간을 정해뒀나?                      [역채점]
       A 분위기 봐서 정한다                        PLN −
       B 몇 시엔 나가야지 하고 왔다                  PLN +

OPN-1  처음 보는 사람들이 섞인 자리다.
       A 새로 온 쪽에 먼저 말을 건다                 OPN +
       B 아는 사람 옆에 붙는다                      OPN −

OPN-2  안 먹어본 안주를 시키자고 한다.               [역채점]
       A 먹던 걸로 하자고 한다                      OPN −
       B 그걸로 시키자고 한다                       OPN +

EMP-1  한 명이 계속 말이 없다.                       [3지선다]
       A 그 사람한테 말을 건다                      EMP +1
       B 옆 사람한테 쟤 원래 저러냐고 물어본다        EMP  0
       C 분위기 좋으니 그냥 둔다                    EMP −1

EMP-2  자리 분위기와 내 컨디션이 부딪힌다.            [역채점]
       A 피곤하면 조용히 있는다                     EMP −
       B 피곤해도 분위기는 맞춘다                   EMP +

MT_RCT-1  누가 웃긴 얘기를 했다.
          A 크게 웃고 되받는다                     MT_RCT +
          B 웃고 듣는다                            MT_RCT −

MT_RCT-2  누가 자기 얘기를 길게 한다.                [역채점]
          A 조용히 듣는다                          MT_RCT −
          B 중간중간 리액션을 넣는다                 MT_RCT +

MT_CRV-1  나를 처음 본 사람은 나를 어떻게 볼까?
          A 밝고 재밌는 사람                       MT_CRV +
          B 조용한데 알수록 괜찮은 사람              MT_CRV −

MT_CRV-2  자리가 끝날 때쯤 나는?                     [역채점]
          A 처음보다 말이 많아져 있다                MT_CRV −
          B 처음이 제일 텐션이 높았다                MT_CRV +

MT_STM-1  자정이 넘었다.                            [순서형]
          A 아직 한참 남았다                       MT_STM +1
          B 한 시간쯤 더                           MT_STM +0.33
          C 슬슬 마무리                            MT_STM −0.33
          D 이미 갔어야 했다                       MT_STM −1

MT_STM-2  2차 가자는 얘기가 나왔다.                  [역채점]
          A 나는 여기까지                          MT_STM −
          B 당연히 간다                            MT_STM +
```

## 9-5. 낮 `DY` — 18문항

```
DOM-1  주말에 뭐 할지 안 정했다.
       A 내가 코스를 짜서 보낸다                   DOM +
       B 뭐 하고 싶은지 먼저 묻는다                 DOM −

DOM-2  식당에서 메뉴를 못 정하고 있다.               [역채점]
       A 먹고 싶은 거 고르라고 한다                 DOM −
       B 이거 시키자고 정한다                      DOM +

SPD-1  상대가 마음에 든다.
       A 바로 말한다                              SPD +
       B 확실해질 때까지 둔다                      SPD −

SPD-2  다음에 만날 날을 잡아야 한다.                 [역채점]
       A 서로 일정 보고 나중에 정한다               SPD −
       B 헤어지기 전에 정하고 간다                  SPD +

EXP-1  서운한 일이 있었다.
       A 그날 바로 말한다                          EXP +
       B 티 안 내고 넘긴다                         EXP −

EXP-2  받은 선물이 취향이 아니다.                    [역채점]
       A 고맙다고 하고 안 쓴다                     EXP −
       B 이런 게 더 좋다고 말한다                   EXP +

PLN-1  여행이 잡혔다. 짐은 언제부터 싸나?             [순서형]
       A 한 달 전                                PLN +1
       B 일주일 전                               PLN +0.33
       C 3일 전                                  PLN −0.33
       D 하루 전날                               PLN −1

PLN-2  데이트 당일 아침이다.                         [역채점]
       A 만나서 정하면 된다                        PLN −
       B 어디 갈지 다 정해뒀다                     PLN +

OPN-1  늘 가던 데가 편하다.
       A 새로운 데 찾아보자고 한다                  OPN +
       B 좋았던 데 또 가자고 한다                   OPN −

OPN-2  상대가 자기 취미를 같이 해보자고 한다.          [역채점]
       A 나는 내 거 하는 게 낫다                    OPN −
       B 한번 해보자고 한다                        OPN +

EMP-1  연인이 차 사고가 났다. 전화가 왔다. 첫마디는?   [3지선다]
       A 몸은 괜찮아? 많이 놀랐지                  EMP +1
       B 112에 신고는 했어? 지금 도로 위야?          EMP  0
       C 사고 규모가 어느 정도야? 차는 얼마나 망가졌어?  EMP −1

EMP-2  상대가 힘든 일이 있었다고 한다.
       내가 봤을 땐 해결법이 보인다.                 [역채점]
       A 방법을 말해준다                           EMP −
       B 말 안 하고 듣기만 한다                     EMP +

DY_FRQ-1  하루 종일 연락이 없었다.
          A 무슨 일 있나 싶다                      DY_FRQ +
          B 바쁜가 보다 한다                       DY_FRQ −

DY_FRQ-2  하루에 연락은 몇 번쯤이 좋나?              [순서형]
          A 계속 이어져야 한다                     DY_FRQ +1
          B 아침 · 점심 · 저녁                     DY_FRQ +0.33
          C 하루 한두 번                          DY_FRQ −0.33
          D 볼일 있을 때만                        DY_FRQ −1

DY_AFF-1  좋아하는 마음을 어떻게 보이나?
          A 말로 자주 한다                        DY_AFF +
          B 챙겨주는 걸로 보인다                    DY_AFF −

DY_AFF-2  상대가 기분이 안 좋아 보인다.              [역채점]
          A 좋아하는 걸 사다 준다                  DY_AFF −
          B 괜찮냐고 계속 물어본다                  DY_AFF +

DY_RCV-1  다투고 헤어졌다.
          A 자기 전에 연락해서 푼다                 DY_RCV +
          B 하루 지나고 얘기한다                    DY_RCV −

DY_RCV-2  화가 났을 때 나는?                         [역채점]
          A 혼자 좀 있어야 한다                    DY_RCV −
          B 그 자리에서 얘기해야 한다                DY_RCV +
```

## 9-6. 밤 `NT` — 18문항

> **노골적인 표현은 쓰지 않는다. 상상하게 두되 문장은 은근하게.** 러브젠가를 수위 기준으로 삼는다.

```
DOM-1  분위기가 만들어졌다.
       A 내가 먼저 움직인다                        DOM +
       B 상대가 움직이길 기다린다                   DOM −

DOM-2  오늘 뭘 할지 정해야 한다.                     [역채점]
       A 상대가 원하는 쪽으로                      DOM −
       B 내가 정해서 말한다                        DOM +

SPD-1  할까 말까 싶은 순간이 왔다.
       A 일단 한다                                SPD +
       B 한 박자 쉰다                             SPD −

SPD-2  상대가 신호를 준 것 같다.                     [역채점]
       A 확실해질 때까지 본다                      SPD −
       B 맞다고 보고 움직인다                      SPD +

EXP-1  원하는 게 있다.
       A 말한다                                   EXP +
       B 티를 낸다                                EXP −

EXP-2  상대가 한 게 별로였다.                        [역채점]
       A 그냥 넘어간다                            EXP −
       B 다음엔 이렇게 하자고 말한다                EXP +

PLN-1  오늘 밤을 어떻게 그렸나?
       A 대충 그려둔 게 있다                       PLN +
       B 아무 생각 없이 왔다                       PLN −

PLN-2  분위기용 준비를 해두는 편인가?                 [역채점]
       A 그때그때 맞춘다                           PLN −
       B 미리 챙겨둔다                             PLN +

OPN-1  상대가 안 해본 걸 제안했다.
       A 재밌겠다                                 OPN +
       B 조금 부담된다                            OPN −

OPN-2  늘 하던 대로가 편한가?                        [역채점]
       A 편한 게 제일이다                          OPN −
       B 가끔은 바꿔보고 싶다                      OPN +

EMP-1  상대가 오늘 좀 지쳐 보인다.                    [3지선다]
       A 오늘은 그냥 쉬자고 한다                   EMP +1
       B 뭐 필요한 거 없냐고 묻는다                 EMP  0
       C 기분 풀리게 해주고 싶다                    EMP −1

EMP-2  내가 원하는 것과 상대 컨디션이 부딪힌다.        [역채점]
       A 오늘은 내가 하고 싶은 대로                 EMP −
       B 상대 컨디션에 맞춘다                      EMP +

NT_MOD-1  분위기가 안 잡힌 날이다.
          A 그래도 상관없다                        NT_MOD +
          B 그런 날은 그냥 넘어간다                 NT_MOD −

NT_MOD-2  조명이나 음악 같은 게 중요한가?             [역채점]
          A 있으면 확실히 다르다                   NT_MOD −
          B 그런 건 별로 상관없다                   NT_MOD +

NT_LED-1  누가 이끄는 편인가?
          A 대체로 내가                           NT_LED +
          B 대체로 상대가                          NT_LED −

NT_LED-2  상대가 이끌어줄 때는?                      [역채점]
          A 편하고 좋다                           NT_LED −
          B 내가 하는 게 낫다                      NT_LED +

NT_OPN-1  이런 얘기를 꺼내는 게?
          A 별로 안 어렵다                        NT_OPN +
          B 조금 쑥스럽다                          NT_OPN −

NT_OPN-2  하고 싶은 걸 말해본 적이 있나?             [순서형]
          A 자주 말한다                           NT_OPN +1
          B 몇 번 말해봤다                        NT_OPN +0.33
          C 말해볼까 하다 만다                     NT_OPN −0.33
          D 말한 적 없다                          NT_OPN −1
```

---

# 10. 게임 구성

## 10-1. 공통 Stage 인터페이스

**게임 담당이 가장 먼저 만들 것은 개별 게임이 아니라 이 인터페이스다.**

```
Stage
├─ id            : string
├─ frame         : "MANY" | "PAIR"
├─ category      : ("TP"|"MT"|"DY"|"NT")[]
├─ duration_sec  : int
├─ offline       : OfflineAction | null
├─ inputs        : 참가자에게서 무엇을 받나
└─ emits         : AxisDelta[] | LogEvent[]

AxisDelta                        LogEvent
├─ axis_id  : string             ├─ kind : "minority_pick" | "telepathy"
├─ source   : "BEHAVIOR"         │         | "me_too" | "guess" | "first_move"
│            | "IMPRESSION"      │         | "ordering"
└─ delta    : float              └─ payload : object
```

> **모든 단계는 `emits`를 반드시 낸다.** 아무것도 안 내는 단계는 게임이지 측정이 아니다.

## 10-2. 다대다 프레임 `MANY` — 팀플 · 합석

| 단계 | 이름 | 시간 | 오프라인 | 채우는 것 | 출처 |
|---|---|---|:---:|---|---|
| 0 | 설문 | 3분 | — | 공통 6 + 전용 3 | `SELF` |
| 1 | 이지선다 + **순서 정하기** | 5분 | ○ | 매치율 · `OPN` `SPD` **`PLN`** | `BEHAVIOR` |
| 2 | 텔레파시 | 5분 | ○ | 케미 · `EMP` `EXP` | `BEHAVIOR` |
| 3 | 나 이런 사람이야 | 5분 | ○ | `OPN` · 공통점 | `SELF` |
| 4 | 누가 했을까 | 5분 | — | 인식 격차 | `IMPRESSION` |

### 0단계 · 설문
- 각자 폰에서 18문항. **제출 전에는 남의 답이 안 보인다**
- 호스트는 미제출자를 건너뛸 수 있다
- **축 이름이나 점수를 절대 노출하지 않는다** — 보이면 답을 조작한다

### 1단계 · 이지선다 동시 공개 + 순서 정하기
- 취향 이지선다 **8개** + **순서 정하기 2개**
- 전원 답하면 **동시 공개.** 누가 뭘 골랐는지 화면에 뜬다
- **오프라인 삽입:** 3번째 문항 후 — *"지금 A 고른 사람 손 드세요"*
- 산출 — 매치율 매트릭스 `N×N` · `minority_pick` 로그 · `OPN` `SPD` 보정 · **`PLN` 보정**

### 1단계 · 순서 정하기 — `PLN`을 행동으로 재는 장치 `D21`

```
┌────────────────────────────┐
│  이 5개를 할 순서대로 놓으세요 │
│  ⏱ 90초                    │
│                            │
│  ☰ 자료 조사               │
│  ☰ 발표 자료 만들기         │
│  ☰ 역할 나누기             │
│  ☰ 초안 쓰기               │
│  ☰ 리허설                  │
└────────────────────────────┘

측정하는 것 — 결과가 아니라 과정이다
  · 첫 입력까지 걸린 시간     계획형은 전체를 훑고 시작한다
  · 재배열 횟수              즉흥형은 일단 놓고 계속 바꾼다
  · 남은 시간 대비 완료 시점

emits: PLN (BEHAVIOR) · LogEvent{ kind: "ordering" }
```

> **어떻게 배열했는지는 안 본다. 어떻게 배열해 갔는지만 본다.**
> 정답이 없어서 조작할 수 없다. 이게 설문과 종류가 다른 데이터가 되는 이유다.
> 별도 단계를 안 만들고 1단계 안에 들어가므로 구현 비용이 거의 없다.

### 2단계 · 텔레파시
- **매치율 상위 짝**으로 자동 페어링. 홀수면 한 조를 3인으로
- 같은 질문에 각자 답. **같으면 성공.** 5문항
- **오프라인 삽입:** 첫 성공 시 — *"짝이랑 하이파이브"*

### 3단계 · 나 이런 사람이야
- 각자 **본인에 대한 사실 한 줄**을 익명 입력 → 해당되는 사람은 **"나도"**
- **오프라인 삽입:** 문장마다 — *"해당되는 사람 손 드세요"*
- **4단계 입력이 된다**

### 4단계 · 누가 했을까
- 3단계 문장들의 **주인을 지목**한다
- 산출 — `guess` 정답률 · **`IMPRESSION` 축 값 역산** · 인식 격차

## 10-3. 2인 프레임 `PAIR` — 낮 · 밤

| 단계 | 이름 | 시간 | 오프라인 | 채우는 것 | 출처 |
|---|---|---|:---:|---|---|
| 0 | 설문 | 3분 | — | 공통 6 + 전용 3 | `SELF` |
| 1 | 동시 선택 + **순서 정하기** | 5분 | ○ | 일치율 · `OPN` `SPD` **`PLN`** | `BEHAVIOR` |
| 2 | 몰래 예측 | 5분 | — | 이해도 · `EMP` | `BEHAVIOR` |
| 3 | 한 걸음 | 5분 | ●● | `DOM` `EXP` | `BEHAVIOR` + `IMPRESSION` |
| 4 | 마지막 선택 | 3분 | ○ | 최종 보정 | `BEHAVIOR` |

**1단계 순서 정하기 항목은 카테고리 어휘로 바꾼다** — 낮은 데이트 코스 5개, 밤은 오늘 저녁 순서 5개.

### 2단계 · 몰래 예측
- *"상대는 이 질문에 뭐라고 답했을까?"* 5문항
- **밤 모드에서 여기가 가장 세게 작동한다.** 상대에 대한 상상을 강제로 하게 만든다

### 3단계 · 한 걸음 — STT를 대체하는 장치
```
오프라인 액션 1개 수행
        ↓
직후 각자 폰에 한 문항

   "방금 누가 먼저 움직였어요?"
   [ 나 ]  [ 상대 ]  [ 동시에 ]

→ 즉시 비공개. 상대에게 안 보인다
→ 두 답이 일치하면 확신도 높음, 엇갈리면 낮음
→ DOM (IMPRESSION) 가산
```

### 4단계 · 마지막 선택
- `[함께]` 각자 50 / `[혼자]` 내가 100, 상대 0 / 둘 다 혼자면 둘 다 0
- 30초 오프라인 대화 → 동시에 버튼
- **축이 경합할 때(±0.3 이내) 타이브레이크로만 쓴다.** 이 한 번으로 유형을 뒤집지 않는다

## 10-4. 오프라인 액션 카탈로그

**공통 원칙 세 가지**

1. **자리에서 안 일어난다** — 일어서서 마주 보고 대화 같은 건 안 쓴다
2. **3초 안에 끝난다** — 길면 부담이 된다
3. **안 해도 넘어간다** — 타이머 종료 시 자동 진행. 강제하지 않는다

```
단체 TP · MT — 활동형
  · 동시에 한 명을 손가락으로 지목
  · 옆 사람과 하이파이브
  · 해당되는 사람만 손 들기
  · 가장 먼저 손 든 사람이 화면 읽기
  · 다 같이 한 명 쳐다보기

연인 · 낮 DY — 저부담, 접촉 없음
  · 화면 동시에 탭
  · 눈 마주치고 3초
  · 동시에 손가락으로 1~5 표시
  · 상대 얼굴 보고 한 단어 말하기

연인 · 밤 NT — 접촉 허용, 여전히 3초
  · 손바닥 맞대고 동시에 탭
  · 상대 손등에 한 글자 쓰기 (상대가 맞히기)
  · 폰 넘기고 30초 동안 상대가 대신 답하기
  · 이마 맞대고 3초
```

## 10-5. 실시간 동기화 — 폴링으로 충분하다

1단계의 핵심은 **전원이 답할 때까지 아무것도 안 보이다가 동시에 공개**되는 것이다. 이게 없으면 각자 폰에서 따로 노는 것이고, 게임이 성립하지 않는다.

```
GET /rooms/{code}/stage/{n}/state       2초 간격 폴링
  → { submitted: 4, total: 6, revealed: false }
  → 전원 제출되면 revealed: true + 결과 동봉

제출 후에는 대기 화면. 웹소켓은 필요 없다.
```

## 10-6. 커버리지 표 — 어느 단계가 어느 축을 채우나

| 축 | 0 설문 | 1 이지선다·순서 | 2 텔레파시 | 3 나 이런 | 4 누가 했을까 |
|---|:---:|:---:|:---:|:---:|:---:|
| `DOM` | ● | | | | ◐ |
| `SPD` | ● | ● | | | |
| `EXP` | ● | | ● | | ◐ |
| `PLN` | ● | **●** | | | |
| `OPN` | ● | ● | | ● | |
| `EMP` | ● | | ● | | ◐ |
| 전용 3축 | ● | | | | |

`●` SELF 또는 BEHAVIOR · `◐` IMPRESSION만

> **유형 결정 3축(`DOM` `PLN` `EXP`)이 모두 게임 데이터를 받게 됐다.**
> `PLN`이 설문 100%였던 게 `D21`로 해소됐다. 계획성은 사람들이 자기를 가장 과대평가하는 축이라, 자기신고만으로 결정하면 8분면의 세로축이 통째로 부정확해진다.
>
> 전용 3축은 여전히 설문 의존이다. 궁합과 서술에만 쓰이므로 MVP에서는 감수한다.

---

# 11. 리포트 — 코드 1벌 + 사전 4벌

## 11-1. 층 구조

```
[축 레이어]      9축 값 × 카테고리
      ↓
[판정 레이어]    유형 / 칭호 / 궁합 등급          ← 전부 룰. LLM 안 씀
      ↓
[사전 레이어]    카테고리별 표시 문구 (텍스트 4개)
      ↓
[LLM 레이어]    서술 문단 · 궁합 이유 · 팀 총평   ← 세션당 1회
      ↓
[렌더 레이어]    리포트 화면 · 공유 카드          ← 코드 1벌
```

## 11-2. 사전 스키마

```json
{
  "category": "TP",
  "display_name": "팀플",
  "subtitle": "같이 뭘 만들어야 하는 사이",
  "skin": "tp",
  "axis_labels": {
    "DOM": "판을 끌고 간다", "SPD": "일단 지른다",
    "EXP": "할 말은 한다",   "PLN": "정해두고 움직인다",
    "OPN": "새로운 방식",    "EMP": "사람 먼저",
    "TP_DDL": "나눠서 미리", "TP_CFL": "그 자리에서 짚는다",
    "TP_ROL": "맡는다"
  },
  "types": {
    "상하직구": {
      "name": "선빵 불도저",
      "quote": "일단 만들어 왔어요",
      "quote_sub": "아무도 시킨 적 없음",
      "strength": "아무도 안 움직일 때 첫 삽을 뜬다",
      "base_illust": "base_03.png"
    }
  },
  "badges": { "마이웨이": { "desc": "남들 갈 때 혼자 다른 데 감", "tone": "neutral" } },
  "grades": {
    "S": { "name": "소울메이트", "reason_templates": ["..."] },
    "F": { "name": "상극",       "reason_templates": ["..."] }
  }
}
```

**카테고리를 추가한다 = 이 파일을 하나 더 쓴다. 코드는 안 건드린다.**
`base_illust`는 베이스 8종 중 하나를 가리키고, 스킨은 `skin` 값으로 입힌다 (§6-6).

## 11-3. LLM 계약

### 호출 시점
**세션 종료 시 정확히 1회.** 전원 리포트를 배치로 한 번에 생성한다.

### 입력

```json
{
  "category": "TP",
  "session": { "size": 6, "date": "2026-08-27" },
  "members": [{
    "user_id": "…",
    "name": "종훈",
    "type": { "key": "상하직구", "name": "선빵 불도저" },
    "axes": { "DOM": 4.7, "SPD": 4.5, "EXP": 4.1, "PLN": 1.2,
              "OPN": 3.8, "EMP": 2.0,
              "TP_DDL": 1.0, "TP_CFL": 4.2, "TP_ROL": 4.5 },
    "team_avg":   { "DOM": 3.1, "PLN": 2.6 },
    "impression": { "DOM": 3.9, "EXP": 2.8 },
    "badges": ["마이웨이", "첫인상 배신자"],
    "compat": [ { "with": "서연", "grade": "S", "total": 0.84 },
                { "with": "지호", "grade": "F", "total": 0.22 } ],
    "game_log": [
      { "stage": 1, "fact": "소수파 3회. '민초'는 6명 중 혼자" },
      { "stage": 1, "fact": "순서 정하기 — 첫 입력까지 2초, 재배열 7회" },
      { "stage": 2, "fact": "텔레파시 1/5" },
      { "stage": 3, "fact": "'해외 산 적 있다' — 3명이 나도" } ]
  }]
}
```

### 프롬프트 규칙 — 이 다섯 개를 반드시 넣는다

```
1. 넘겨받은 JSON에 있는 수치와 로그만 인용한다. 없는 사실을 만들지 않는다.
   ("회의에서 자주 발표하셨네요" 같은 문장 금지 — 그런 데이터가 없다)

2. 유형 · 칭호 · 궁합 등급은 이미 확정됐다. 바꾸지 않는다. 설명만 한다.

3. impression 값은 "팀이 본 나"다.
   "당신은 ○○입니다"로 쓰지 않는다. "팀은 당신을 ○○로 봤습니다"까지만.

4. 외모 · 성별 · 능력에 대한 문장을 만들지 않는다.

5. 개인 서술은 4~6문장. 궁합 이유는 1~2문장.
   수치를 최소 2개 인용한다. 인용 없는 뭉뚱그린 칭찬은 쓰지 않는다.
```

### 저장과 폴백

```
생성 성공 → DB 저장 → 이후 조회는 저장본만 읽는다
            └ LLM을 다시 부르지 않는다는 뜻이다. 저장은 반드시 한다
            └ 매번 부르면 같은 사람 리포트가 매번 달라진다
            └ 어제 캡처한 것과 오늘 화면이 다르면 신뢰가 통째로 날아간다

생성 실패 → 유형별 템플릿 문구로 폴백
            └ 유형이 같으면 거의 같은 글이 나간다. 감수한다
            └ 축 · 칭호 · 궁합은 룰 기반이라 그대로 채워진다
            └ 리포트가 안 나오는 경우는 없다
```

## 11-4. 무엇이 고정이고 무엇이 다른가

| | 고정 — 유형 같으면 동일 | 변동 — 사람마다 다름 |
|---|---|---|
| 내용 | 유형 이름 · 대사 · 강점 한 줄 · 캐릭터 | 9축 실제값 · 칭호 · 궁합 · 게임 로그 · 인식 격차 |
| 생성 | 사전 파일에서 그대로 | LLM 서술 |
| 노출 | **공유 카드** | **리포트 본문** |

> 캡처되는 카드는 고정 층이라 **유형 정체성이 유지되고**, 열어보는 리포트는 변동 층이라 **내 얘기 같다.**

## 11-5. 궁합 이유 폴백 템플릿

### 단체 `TP` `MT`

```
S · 소울메이트
  "취향 {match}% 일치에 텔레파시 {chem}/5. 이 정도면 의심스럽습니다"
  "생각하는 것도 고르는 것도 거의 같습니다. 팀 안에서 제일 편한 조합"

A · 척하면 척
  "취향은 갈리는데 결정이 빠릅니다. 말이 짧게 끝나는 조합"
  "한 명이 벌이면 한 명이 정리합니다. 잘 굴러갑니다"

B · 무난
  "특별히 부딪히지도, 특별히 통하지도 않습니다"

C · 노력형
  "한 명은 직구, 한 명은 변화구. 오해가 쌓이기 쉬운 조합"

F · 상극
  "둘 다 계획을 안 세웁니다. 마감 전날 같이 밤샐 확률 높음"
  "둘 다 끌고 가려고 합니다. 회의가 길어집니다"
```

### 연인 `DY` `NT` — 반드시 수치를 인용한다

```
S · 환상의 궁합
  "10개 중 {match}개가 같았습니다. 예측도 {guess}/5.
   이 정도면 좀 무섭습니다"

A · 말 안 해도 통함
  "취향은 {match}개만 겹치는데 예측은 {guess}/5입니다.
   좋아하는 건 달라도 서로를 잘 압니다"

B · 평범한 사이
  "일치 {match}개, 예측 {guess}/5. 딱 평균입니다.
   좋게 보면 무난하고, 나쁘게 보면 아직 모릅니다"

C · 맞춰가는 중
  "방향은 같은데 속도가 다릅니다. 한 명이 기다려야 합니다"

F · 환장의 궁합
  "10개 중 {match}개 일치. 그런데 예측은 {guess}/5로 맞혔습니다.
   서로를 잘 알면서 취향만 정반대입니다"
  "둘 다 상대가 리드해주길 기다립니다.
   오늘 저녁 메뉴는 아직 못 정했을 겁니다"
```

---

# 12. MVP 화면 흐름

```
[1] 시작          닉네임 · 성별 입력 · user_id 발급
       ↓
[2] 카테고리      단체 / 연인  →  팀플·합석 / 낮·밤          ← 2단
       ↓
[3] 방 만들기     서버가 발급한 초대코드 6자리 + QR
       ↓
[4] 대기실        참가자 목록 폴링 · 호스트만 [시작]
       ↓
[5] 설문          18문항 · 진행바 · 제출 전 남의 답 안 보임
       ↓
[6] 1단계         이지선다 8 + 순서 정하기 2 → 동시 공개 → 오프라인
       ↓
[7] 2단계         텔레파시 · 짝 자동 배정
       ↓
[8] 3단계         나 이런 사람이야 · "나도" 버튼
       ↓
[9] 4단계         누가 했을까 · 익명 지목
       ↓
[10] 리포트       [내 결과] [팀 궁합] [페르소나 게임]
       ↓
[11] 공유 카드    이미지로 저장
```

**시작 화면 입력은 닉네임과 성별뿐이다** `D22`. MBTI는 받지 않는다 — 우리 축과 겹쳐서 결과 신뢰를 깎고, 계산에 넣으면 4비트짜리 자기신고가 9축 중 하나를 통째로 흔든다.

## 공유 카드

```
┌──────────────────────────────┐
│  TEAM PROJECT · 6인 세션       │
│                              │
│        [캐릭터 · 베이스+스킨]   │
│                              │
│       선 빵 불 도 저           │
│                              │
│      "일단 만들어 왔어요"       │
│       아무도 시킨 적 없음        │
│                              │
│   (마이웨이) (첫인상 배신자)     │
│  ───────────────────────────  │
│   아무도 안 움직일 때           │  ← 강점 한 줄 (D8)
│   첫 삽을 뜬다                 │
│  ───────────────────────────  │
│   최고 궁합 · 서연      S      │
│   최악 궁합 · 지호      F      │
│                              │
│  2026.08.27           얼음땡   │
└──────────────────────────────┘
```

- **정사각형에 가깝게** — 인스타 스토리와 카톡 미리보기 양쪽에서 안 잘린다
- **유형 이름은 크게, 대사는 그 밑에**
- **칭호는 최대 2개**
- **강점 한 줄 반드시 포함** `D8` — 본문에만 두면 남이 보는 화면에서 커버가 안 된다
- **최고·최악 궁합을 같이 넣는다** — **F가 있어야 대화가 시작된다**
- 밤 모드 카드도 **원문 그대로** 나간다 `D12`

## 디자인 컨셉

| 요소 | 규칙 |
|---|---|
| 헤딩 | Black Han Sans — **유형 이름은 화면에서 가장 큰 글자** |
| 본문 | IBM Plex Sans KR |
| 수치 · 코드 | IBM Plex Mono |
| 주색 | 핫핑크 `#C9256B` — 유형 · 강조 · S등급 |
| 보조색 | 딥틸 `#1F4E5F` — F등급 · 중립 정보 · 점선 레이더 |
| 강점 박스 | 골드 `#A8770F` — 회수 문구 전용 |
| 카드 배경 | 다크 `#181017` — **공유 카드만 반전** |
| 다크 모드 | 전 화면 대응 |

---

# 13. 밤 모드 `NT` 전용 사양

| 항목 | 사양 |
|---|---|
| 진입 | 연인 2단 화면에서 선택. **동의 게이트 없음** |
| 첫 화면 노출 | **안 함.** 연인을 눌러야 보인다 |
| 스킵권 | **3장.** 소진되면 스킵 불가 |
| 스킵 노출 | 상대에게 **안 보인다** |
| 문구 수위 | 러브젠가 기준. **은근하게, 노골적이지 않게** |
| 공유 | **허용. 원문 그대로 나간다** |
| 시연 | 밤 모드 화면은 시연에서 안 띄운다. 시연 계정은 `TP`로 |

```
[ 스킵 ]  ● ● ○      ← 남은 2장
```

무제한이면 부담스러운 질문을 전부 피해서 축이 안 쌓이고, 아예 없으면 도망갈 데가 없다. **3장이면 "나 이거 카드 쓴다"가 오히려 웃음 포인트가 된다.**

---

# 14. 파트별 담당과 산출물

## 아이스브레이킹 — 진웅 · 재환

| 산출물 | 참조 |
|---|---|
| 다대다 4단계 시나리오 — 화면 흐름 · 문구 · 타이머 | §10-2 |
| 2인 4단계 시나리오 — 낮 · 밤 | §10-3 |
| 오프라인 액션 최종안 — 카테고리별 5개씩 | §10-4 |
| 1단계 이지선다 8문항 × 4벌 | — |
| **1단계 순서 정하기 항목 5개 × 4벌** | §10-2 |
| 2단계 텔레파시 5문항 × 4벌 | — |

> **오프라인 액션은 반드시 직후 온라인 1문항과 세트로 낸다.** 액션만 있고 문항이 없으면 데이터가 안 남는다.

## 게임 — 치영 · 민우

| 산출물 | 참조 |
|---|---|
| **공통 Stage 인터페이스** — 가장 먼저 | §10-1 |
| **익명 계정 · `user_id` 발급 · `localStorage`** | §4 |
| **`User` / `Participant` 모델 분리** | §4-5 |
| 다대다 프레임 구현 · **폴링 동시 공개** | §10-2 · §10-5 |
| 2인 프레임 구현 | §10-3 |
| 축 산출 엔진 — 3출처 누적 → 0~5 정규화 | §5-4 |
| 궁합 계산 — **가중치 표를 데이터로 읽는다. 하드코딩 금지** | §8-4 |

> **개별 게임보다 인터페이스와 저장이 먼저다.** 저장할 곳이 없으면 게임을 만들어도 결과가 안 남는다.

## 기획 — 종훈 · 기준

| 산출물 | 참조 |
|---|---|
| 축 정의 확정 — 18축 | §5-3 |
| 문항 세트 4벌 — **72문항 완성본** | §9 |
| 사전 파일 4벌 — JSON. 유형 32종 문구 포함 | §11-2 |
| 리포트 화면 명세 | §11 · §12 |
| 가중치 표 튜닝 · **연인 칭호 임계값 실측** | §8-4 · §7-3 |

---

# 15. 예외 케이스

| 상황 | 처리 |
|---|---|
| **한 명이 튕겼다** | **기다리지 않고 진행한다.** 호스트에게 [기다리기] / [넘기기]를 띄우고, 안 돌아오면 그 시점까지의 데이터로 축을 확정한다. 궁합에서도 빼지 않는다 — 있는 데이터로 낸다 |
| **재입장** | `localStorage`의 `user_id`로 복구. **현재 단계부터 이어서**. 지나간 단계는 안 채운다 |
| **새로고침 · 탭 종료** | 위와 동일. `user_id`만 살아 있으면 서버에서 전부 복원된다 (§4-3) |
| **`user_id` 분실** | 새 사람으로 시작. **MVP에서 복구하지 않는다** (§4-4) |
| **참가자 3명 미만** (다대다) | 2단계 짝 매칭 스킵. 1·3·4단계만. 매치율은 생성하되 궁합 `B` 고정 |
| **홀수 인원** | 2단계에서 한 조를 3인으로. 케미는 3인 평균 |
| **설문 미제출** | 호스트가 건너뛰면 전 축 2.5. 유형은 `SPD` 타이브레이크로 결정 |
| **3단계 입력 공란** | 그 사람 문장은 4단계에서 제외. `me_too` 0 |
| **두 문항 모순** | 해당 축 2.5 + `low_confidence`. 리포트에 "이 축은 답이 갈렸습니다" 표시 |
| **순서 정하기 미완료** | `PLN` BEHAVIOR 없음 → `axis_final = SELF` |
| **LLM 실패** | 템플릿 폴백 (§11-3) |
| **밤 모드 스킵권 소진** | 스킵 버튼 비활성. 문항은 계속 진행 |
| **매치율 전원 동률** | 짝을 랜덤 배정. 시드는 `session_id` 고정 (재현성) |

---

# 16. 안전 규칙

바이럴을 노리면 반드시 선을 넘는 사람이 나온다. **선을 코드로 그어둔다.**

1. **뼈 있는 문구는 `SELF` / `BEHAVIOR` 기반으로만 만든다**
   본인이 답한 것, 본인이 게임에서 실제로 한 것. **내가 답한 것이라 웃고 넘긴다**

2. **`IMPRESSION` 기반은 중립 표현만**
   "팀은 당신을 ○○로 봤습니다"까지. **"당신은 ○○입니다"로 바꾸지 않는다**

3. **외모 · 성별 · 능력 관련 문구는 생성하지 않는다**

4. **강점 한 줄로 회수한다** `D8`
   세게 찌른 만큼 마지막 줄에서 돌려준다. 공유 카드에도 반드시 넣는다

5. **긍정 칭호를 최소 1개 보장한다** `D9`

> **칭호는 팀에 공개되고 숨길 수 없다** `D11`.
> 그래서 4번과 5번이 유일한 완충 장치다. **이 둘은 절대 빼지 않는다.**

> `"게을러 보인다 4표" → lazy = 95` 는 금지다.
> **재밌게 만드는 것과 타인의 인상을 사실로 확정하는 것은 다르다.**

---

# 17. 미결

| # | 항목 | 왜 미결인가 | 필요 시점 |
|---|---|---|---|
| O1 | 캐릭터 베이스 8종 + 스킨 4벌 | 방향은 정함(`D16`). 제작 미착수 | 카드 구현 전 |
| O2 | 1단계 이지선다 8문항 × 4벌 | 취향 문항. 미작성 | 1단계 구현 전 |
| O3 | 순서 정하기 항목 5개 × 4벌 | 카테고리별 어휘로 | 1단계 구현 전 |
| O4 | 2단계 텔레파시 5문항 × 4벌 | 미작성 | 2단계 구현 전 |
| O5 | 연인 칭호 임계값 실측 | §7-3 숫자에 근거가 없다. 목표는 발생률 20~40% | 연인 착수 시 |
| O6 | 가중치 표 튜닝 | 시연에서 돌려보고 조정 | 팀플 검증 후 |
| O7 | `EMP` 분산 검증 | 3지선다로 갈리는지 실측. **표준편차 0.8 미만이면 축으로 못 쓴다** | 팀플 검증 시 |
| O8 | 전용 3축 게임 단계 | 여전히 설문 의존 (§10-6) | P1 |

**해소된 것** — 문항 72개 확정(§9) · 서비스 이름(`D18`) · 계정 방식(`D17`) · `PLN` 게임 단계(`D21`) · 카테고리 간 궁합 폐기(`D20`) · MBTI 폐기(`D22`) · 칭호 숨김 폐기(`D11`)

---

# 18. 구현 순서 `D14`

```
1주차 ─ 팀플만 수직으로 뚫는다
        ① 익명 계정 · user_id · User/Participant 분리
        ② 방 생성 · 입장 · 대기실 (서버 연동 + 폴링)
        ③ 설문 18문항
        ④ Stage 인터페이스 + 1단계 (이지선다 + 순서 정하기 + 동시 공개)
        ⑤ 축 산출 → 유형 판정 (룰만)
        ⑥ 2~4단계 → 칭호 · 궁합 → 리포트 → 공유 카드
        └ 여섯 명이 그대로 참가자다. 실제로 돌려본다

2주차 ─ 수평으로 넓힌다
        합석  사전 파일 + 문항 교체. 프레임 재사용 (거의 공짜)
        연인  2인 프레임 착수. 낮·밤은 사전만 다르므로 사실상 한 벌
```

> **①②가 끝나야 나머지가 붙는다.** 저장할 곳이 없으면 게임을 만들어도 결과가 안 남는다.
> **팀플이 돌면 합석은 사전 파일 하나다.** 순서를 바꾸면 프레임 두 벌을 동시에 디버깅하게 된다.
